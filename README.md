
1 )Clone project:

   
2) Install dependencies:

    $ cd angular-material-dashboard
    $ npm install

3) Install gem 'sass'

    $ gem install sass
    
4) Run development web-server:

    $ gulp serve
